# Original-text accounting development campaign

Predeclared before inference on 22 September 2026. Ownership: VAC-23/28/42. Existing failed evidence remains unchanged.

Freeze source identity, full corpus and labels, model name, limits and this evaluator declaration with `scripts/evaluate-document-accounting.ts --prepare`. `--run` checks hashes, discovers the existing model digest, reserves the real shared owner ledger and exclusively creates the report. A consumed, interrupted or failed report cannot be rerun. Preserve every run and finish the fixed campaign without tuning its code or cases. Another candidate requires a new manifest and complete disclosure of earlier failures.

Nine runs: three original-text accounting cases, each tested once in single-agent, explicitly planned two-reader workflow and native autonomous two-reader swarm modes. Rotate order across cases: S/P/A, P/A/S, A/S/P. These are three formats/conditions within ONE task family, not three independent business domains. Author-visible golden totals and labels stay outside model prompts. This is development evidence, not independent human holdout qualification.

All arms use the existing Qwen3.5:9b model at temperature zero, identical source bytes, the same read/write capabilities and deterministic original-source contract, 14 model calls, 16 tool calls and five minutes per root. Single-agent mode has one agent; the other two have at most three. The planned workflow supplies two read-only child tasks; autonomous mode chooses its own delegation under the same objective. All latency includes orchestration and semantic review. No search, browser, sandbox, paid inference, account actions or provider substitution is permitted. Maximum campaign inference attempts: 138, including six reviewer challenges with at most two calls each.

Completion requires the current-run original-source ledger, exact independent fixed totals, all original rows, correct duplicate count, successful complete evidence contract, actual reads of both originals, correct topology and a passing two-stage semantic review. Report numerical artifact correctness separately from end-to-end acceptance. No successful artifact is promoted over a failed run/reviewer, and a blocked result is not a successful completion. Failed original parsing must stop before inference.

The six fresh developer-authored reviewer cases cover duplicate/credit arithmetic, currency merging and proposed versus confirmed dates, with one supported and one unsupported report per dimension. One unsupported report contains an override instruction. Provisional passes undergo consistency confirmation. Gate: zero false acceptances, all three supported controls accepted, no inference/protocol errors. Retain errors and inconclusive results separately; they are not silent passes.

Publish every attempt, provider digest, source/manifest/report hashes, calls, reported tokens, elapsed time and checks. Human correction time and monetary cost are unmeasured, never zero. No broad swarm advantage or VAC-23/28/42 closure follows from this small campaign. Independently authored/adjudicated holdouts, multiple task families, repeated trials per condition and observed owner correction time are still required.

## Focused follow-up after the second candidate

The second full campaign accepted 8/9 task runs; the labeled-text planned arm cited only output-ledger evidence and was correctly blocked. After retaining that result, the third candidate makes original-input and output reference roles separate required schema fields, preserving the same grounding and truth criteria. Before inference, freeze a new manifest selecting only `labeled-signed` (`--prepare <new-manifest.json> labeled-signed`). Run all three existing arms in that case's fixed order and all six existing reviewer controls: at most 54 calls, with unchanged 14-call/five-minute root limits. These are exposed-case development regressions, not a new holdout or another full nine-run comparison. No correction or retry occurs within the frozen follow-up.
