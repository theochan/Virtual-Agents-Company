# Additional direct single-agent baseline

Frozen before execution of this baseline, while the previously frozen fresh confirmation runs. The development checkpoint baseline failed repeated writes; this simpler comparator tests whether that workflow overhead exaggerates the apparent quality advantage. The objective is shorter, with no scratch instructions, and the required sequence is read Harbor, read Summit, write result.json, finish. Same fresh sources, model, root caps, output fields and external source oracle; one attempt per case, at most 42 calls total. No simultaneous inference with the main campaign.

Report all three results. For a robust bounded quality claim, planned must still pass all three fresh cases and neither single baseline may exceed one correct case. If direct single does better, report that limitation rather than selecting the weaker comparator. This is an additional stricter check, not a relaxation of the original gate.

Driver: data/evaluations/2026-09-23-direct-single-baseline-driver.mts (exact archived bytes). Driver SHA256: cc13bbfd9545b8e863b60edeecccaa902c2b61ea8338bbbcbe6842664aa6cb8c
