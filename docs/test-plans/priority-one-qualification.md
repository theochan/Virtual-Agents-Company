# Priority 1 qualification contract — 22 September 2026

Owners: VAC-23 planning, VAC-28 semantic correctness, VAC-42 comparative usefulness. No scaling, deployment, provider substitution or raised allowance is part of this work.

## Current bounded campaign

The owner's shared UTC allowance was inspected: 62/100 inference attempts consumed, 38 remaining. Full MSFT acceptance needs at least 57 calls and is inadmissible today under that snapshot. Use at most 36 calls for the following pilot, with all reservations charged to the real owner ledger and all trial files isolated. Search and external application writes are disabled.

Before inference, freeze source hash, corpus, labels, model/configuration, output rubric and caps with `scripts/evaluate-priority-one.ts --prepare`. `--run` refuses changed source/declarations, prior result paths, unavailable model, or insufficient worst-case daily allowance. Preserve model digest, exact evidence, failures, partial results and budget snapshots. Stop at a failed candidate; do not silently rerun or edit the corpus after seeing output.

1. Reviewer challenge: eight newly authored cases, one attempt each, covering unit conversion, estimated versus confirmed dates, credits and invoices, superseded source evidence and an embedded override instruction. Four supported and four unsupported reports. Expected labels stay out of provider prompts. Acceptance: zero false acceptances, at least three of four correct reports accepted, zero inference/protocol errors. Inconclusive on a bad report is safe rejection; it is not a precise diagnosis. This is a developer-authored held-out challenge relative to previous development examples, not an independently human-authored external holdout.
2. Paired pilot: one identical synthetic recurring invoice-reconciliation task, same two source files, Qwen3.5:9b, temperature zero, three tool types, 14 model calls per arm including review, and same input/output/deadline caps. Single arm cannot delegate; swarm arm generates its own two-specialist plan. Only architecture-specific role instructions differ. Both must read evidence, calculate, produce current-run result.json and pass isolated semantic review. Exact expected fields/values are checked outside the model and are not exposed as artifact contracts. Ground truth is integer arithmetic with deduplication and credit subtraction.
3. Record completions, artifact contents/hashes, semantic review, model/tool attempts, reported tokens, wall time and all failed cases. Monetary costs and owner correction time remain unknown, not zero. Any faster successful run is descriptive only: fixed order, caching and one task cannot establish causal speedup or general usefulness. No general advantage declaration is permitted from this pilot.

## Remaining promotion work

The MSFT acceptance contract remains unchanged, including independent citation/meaning audit, three fresh repetitions on a frozen candidate and a separately prepared ticker/brief. Today's invoice pilot cannot replace it.

After the pilot, predeclare a broader held-out corpus with multiple recurring task families, sufficient repetitions and randomized/counterbalanced arm order. Include single equipped agent, owner-planned workflow and autonomous swarm. Measure owner correction time directly. The missing owner-plan arm and small sample are explicit pilot limitations, not a waived VAC-42 requirement.

VAC-28 still requires broad semantic rubrics, independently adjudicated labels, correlated-error analysis and an owner adjudication path. VAC-32 owns longitudinal evidence; it is not closed by a short run. Do not spend paid provider/data credit or modify scheduled work to complete these gates.
